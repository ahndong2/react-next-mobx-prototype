import { SampleMobx } from '@/containers';
import { DefaultLayout } from '@/components';
const Sample = () => {
  return (
    <DefaultLayout>
      <SampleMobx />
    </DefaultLayout>
  );
};

export default Sample;
