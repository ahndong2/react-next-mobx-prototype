import { SampleContainer } from '@/containers';
import { DefaultLayout } from '@/components';
const Sample = () => {
  return (
    <DefaultLayout>
      <SampleContainer />
    </DefaultLayout>
  );
};

export default Sample;
